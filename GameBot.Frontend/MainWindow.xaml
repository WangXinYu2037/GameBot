<Window x:Class="GameBot.Frontend.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameBot.Frontend"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="250">
    <Window.Resources>
        <Style x:Key="FlatButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Border x:Name="MainBorder" Background="#4CAF50" CornerRadius="8" Width="120" Height="45" 
                HorizontalAlignment="Center" VerticalAlignment="Center"
                MouseEnter="Button_MouseEnter" MouseLeave="Button_MouseLeave">
            <Button x:Name="MainButton" Content="开始" Foreground="White" Width="120" Height="45" 
                    Click="MainButton_Click" Style="{StaticResource FlatButtonStyle}"/>
        </Border>
    </Grid>
</Window>
